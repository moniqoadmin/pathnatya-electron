/**
 * Replaced at build time by electron-vite `define` in both the main and renderer
 * bundles. True only for the windowed build variant (`PATHNATYA_WINDOWED=1`),
 * which plays the video inside a small app window instead of forcing fullscreen.
 */
declare const __PATHNATYA_WINDOWED__: boolean
